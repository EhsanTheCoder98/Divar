const defaultOptions = {
    queries:{
        gcTime:3000,
        staleTime:60000,
        refetchOnWindowFocus:false,
        refetchOnMount:false,
        retry:1,
      }
}

export {defaultOptions};